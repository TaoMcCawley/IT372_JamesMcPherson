package launcher;

import java.util.ArrayList;

public class Main {

    public static void main(String[] args) {
        //Instantiates an arrayList called lineList and equals it to the readLines method
        //circularshift all the lines
        //sort all the lines
        //write the lines in lineList to file and console
    }

    public static ArrayList<String> readLines(){
        //Starts a new ArrayList called lineList
        //Opens the file, reads it, and adds the individual lines to the lineList
        return lineList;
    }

    public static void circularShift(ArrayList<String> lineList){
        //for every line in the lineList
            //Call the circularShiftLine Method to shift the words in each line
    }

    public static String circularShiftLine(String line){
        //Shifts the words in a line, returns it to the circular shift method
        return theNewShiftedString;
    }

    public static void alphabetize(ArrayList<String> lineList){
        //sorts all the lines in line list
    }
}

